# Keeps the build context small and, more importantly, keeps secrets and the
# host's virtualenv out of the image.
.env
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
Dockerfile
.dockerignore
